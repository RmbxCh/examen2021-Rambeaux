{% extends 'base.html.twig' %}

{% block title %}
    Ajouter un genre
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ajouter un genre</h1>
        {{ form_start(formGenre) }}
            {{ form_label(formGenre)}}
            </br>
            {{ form_help(formGenre, {'attr': {'class': 'form-text text-muted mt-2'}}) }}
            {{ form_widget(formGenre, {'attr': {'class': 'mb-3'}})}}
        {{ form_end(formGenre) }}
    </div>
{% endblock %}